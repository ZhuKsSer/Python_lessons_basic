#EASY
# Задача-1:
# Напишите скрипт, создающий директории dir_1 - dir_9 в папке,
# из которой запущен данный скрипт.
# И второй скрипт, удаляющий эти папки.

import os
import shutil

name = ''

# создание директорий
def new_path(name):
   path = os.path.join(name)
   try:
       os.makedirs(path)
   except OSError:
       print("Создать директоию %s не удалось" % path)
   else:
       print("Успешно создана директория %s " % path)

for i in range(1,10):
   new_path('{}_{}'.format('dir', i))

# удаление директорий
def path_remove(path_del):
   try:
       os.removedirs(path_del)
   except OSError:
       print ("Удалить директорию %s не удалось" % path_del)
   else:
       print ("Успешно удалена директория %s" % path_del)

   for i in range(1,10):
 path = '{}_{}'.format('dir', i)
print(path)
path_remove(path)


# Задача-2:
# Напишите скрипт, отображающий папки текущей директории.

cur_dir = [os.listdir()]
print(cur_dir)


# Задача-3:
# Напишите скрипт, создающий копию файла, из которого запущен данный скрипт.

i = 0
while i < len(cur_dir):
   if os.path:
       new_file = cur_dir[i] + '.dupl'

       shutil.copy(cur_dir[i], new_file)
       i += 1
